# Django User Form Project

Простой проект на Django для демонстрации работы с формами.  
Позволяет пользователю ввести имя, email и пароль, проверяет правильность ввода и показывает результат.

---

## 📋 Функционал

- Отображение формы для ввода данных (`name`, `email`, `password`)
- Валидация данных (проверка на пустые поля, корректность email и т.д.)
- Показ ошибок прямо на странице при некорректном вводе
- Отображение введённых данных при успешной отправке

---

## 🛠 Технологии

- Python 3.x  
- Django 4.x (или ваша версия)  
- HTML / Django Templates  

---

## 🚀 Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone <ссылка_на_репозиторий>
   cd <название_проекта>
Создайте виртуальное окружение и активируйте его:

bash
Копировать код
python -m venv venv
source venv/bin/activate   # для Linux/macOS
venv\Scripts\activate      # для Windows
Установите зависимости:

bash
Копировать код
pip install -r requirements.txt
Примените миграции:

bash
Копировать код
python manage.py migrate
Запустите сервер:

bash
Копировать код
python manage.py runserver
Откройте браузер и перейдите по адресу:

cpp
Копировать код
http://127.0.0.1:8000/
## 📂 Структура проекта

project_name/
├── project_name/
│   ├── __init__.py
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── app_name/
│   ├── __init__.py
│   ├── forms.py          # форма пользователя
│   ├── views.py          # логика обработки формы
│   ├── models.py
│   ├── admin.py
│   ├── apps.py
│   ├── templates/
│   │   └── form.html     # HTML-шаблон формы
│   └── migrations/
│       └── __init__.py
├── manage.py
└── requirements.txt

⚡ Использование
Откройте страницу с формой

Введите имя, email и пароль

Нажмите кнопку "Отправить"

Если данные корректны — увидите их на экране
Если есть ошибки — они отобразятся рядом с полями

📝 Пример кода формы
python
Копировать код
# views.py
def form(request):
    if request.method == 'POST':
        userform = UserForm(request.POST)
        if userform.is_valid():
            name = userform.cleaned_data['name']
            email = userform.cleaned_data['email']
            password = userform.cleaned_data['password']
            return HttpResponse(f"Name: {name}, Email: {email}, Password: {password}")
        else:
            field_errors = userform.errors.get_json_data()
            return render(request, 'form.html', {'field_errors': field_errors})
    else:
        userform = UserForm()
    return render(request, "form.html", {"form": userform})
📌 Примечания
Убедитесь, что UserForm правильно определён в forms.py

Для реальных проектов не храните пароли в открытом виде — это небезопасно.

🧑‍💻 Автор
Мерхат
